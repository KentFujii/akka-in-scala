name := "akka-in-scala"

version := "1.0"

lazy val upAndRunning = project.in(file("up-and-running"))
lazy val test = project.in(file("testdriven"))
lazy val faulttolerance = project.in(file("fault-tolerance"))
lazy val futures = project.in(file("futures"))
